#include "main.h"

/**
  * _islower - checks if letter are lowercase
  *
  * @c: this is a function hat holds (int c) as a parameter
  *
  * Return: 1 or 0 depending on the outcome of condition
  */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
