#include <stdio.h>

/**
 * main - prints it's name
 * @argc: count
 * @argv: pointer to string input
 *
 * Return: (0) when successful
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
